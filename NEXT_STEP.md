# Next step

Upload the CONTENTS of this folder to the root of the GitHub repository and commit to `main`. GitHub Actions will run **Build Android APK** automatically. When the run has a green check mark, download artifact **ShopeeNumberFinder-phase1-debug**, extract it, and install `app-debug.apk` on Android.
